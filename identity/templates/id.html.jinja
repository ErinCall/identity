{% extends 'layout.html.jinja' %}
{%block head%}
    <link rel="openid.server" href="{{ base_url }}openidserver">
{%endblock head%}
{%block content%}
    <div>
        {% if approved_trust_roots %}
        <p>Approved Trust Roots:</p>
        <ul>
            {% for trust_root in approved_trust_roots %}
                <li>{{ trust_root }}</li>
            {% endfor %}
        </ul>
        {% endif %}
    </div>
{%endblock content%}
